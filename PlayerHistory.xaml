<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Project2A.PlayerHistory"
             Title="PlayerHistory">
    <ScrollView>
        <StackLayout>
            <Grid x:Name="TopGrid" Padding="10,10,10,10" RowSpacing="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Entry Grid.Row="0" x:Name="PlayerNameLabel" TextChanged="OnPlayerNameChanged"></Entry>
                <Label Grid.Row="1" x:Name="roundCounter" VerticalOptions="End" HorizontalOptions="End"></Label>
                <Label Grid.Row="2" x:Name="guessCounter" VerticalOptions="End" HorizontalOptions="End"></Label>
            </Grid>
            <StackLayout x:Name="container1" Spacing="0" Padding="0" WidthRequest="100" VerticalOptions="Start" HorizontalOptions="Start" Margin="10,0,0,0">
            </StackLayout>
        </StackLayout>
    </ScrollView>
</ContentPage>